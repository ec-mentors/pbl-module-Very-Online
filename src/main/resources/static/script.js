document.addEventListener('DOMContentLoaded', () => {
    console.log("Page loaded. Server-side rendering is active.");
    // All screen-switching logic is removed.
    // You would only add back specific, client-side logic here.
    // For example, the code to handle the volume slider.
});